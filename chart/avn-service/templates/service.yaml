apiVersion: v1
kind: Service
metadata:
  name: {{ include "avn-service.fullname" . }}
  labels:
    {{- include "avn-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avn-service.selectorLabels" . | nindent 4 }}
