apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nft-service.fullname" . }}-test-connection"
  labels:
    {{- include "nft-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nft-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
